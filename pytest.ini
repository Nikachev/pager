[pytest]
pythonpath = tests .
addopts = --tb=short
markers =
    smoke: Fast non-destructive transport and HTTP checks (< 20s total)
    contract: Extended non-destructive HTTP, keyboard, and OTA error-path checks
    dfu: Heavy HTTP/Serial DFU firmware self-flashing tests
    ble: Bluetooth Low Energy GATT and HID checks
