# Optional local environment overrides for Makefile
# Copy this file to .env to apply custom settings

DEVICE_IP=192.168.42.1
PORT=/dev/cu.usbmodem123456783
# Leave BIN unset to use the slot-specific default, e.g. dist/pager-A.bin.
# BIN=dist/pager-A.bin
PROBE_RS=probe-rs
